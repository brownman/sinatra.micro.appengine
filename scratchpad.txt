# specific routes for the production environment so that we do not reveal too much information on what went wrong...
configure :production do
  not_found do
    status 404 # or simply return nothing ...  
  end

  error do
    status 500 # or simply return nothing ...
  end  
end

load 'routes/route1.rb'
load 'routes/route2.rb'

# launch standard sinatra, without using ::Base
run Sinatra::Application


%link{:href => '/main.css', :rel => 'stylesheet', :type => 'text/css'}